export enum RequestError {
  Other = 'Other',
  Unauthorized = 'Unauthorized',
  Conflict = 'Conflict',
  Gone = 'Gone'
}

export enum ResponseType {
  Json = 'Json',
  Text = 'Text',
  None = 'None'
}
